#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @n: is the number to be checked
 * Return: the absolute value for the input number
 */
int _abs(int n)
{

int result = abs(n);
return (result);

}
