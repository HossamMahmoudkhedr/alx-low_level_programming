#include <stdio.h>
#include "main.h"

/**
 * main - test if the postive_or_negative function returns a right value with 0
 * 
 * Return: 0
 */
int main(void)
{

    int i;
    i = 0;

    positive_or_negative(0);
    return (0);

}
