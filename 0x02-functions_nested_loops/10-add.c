#include <stdio.h>
/**
 * add - adds two integers and returns the result
 *
 * @x: the first parameter to be added to the second parameter
 * @y: the seconde parameter to be added to the first parameter
 * Return: the result of the sum of the two integers 
 */
int add(int x, int y)
{
int result = x + y;
return (result);
}
